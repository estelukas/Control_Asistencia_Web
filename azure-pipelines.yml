trigger:
- ControlAsistencia  # 🔹 Habilita CI en la rama principal

pool:
  vmImage: 'windows-latest'
  #name: Default

variables:
  solution: '**/*.sln'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'

steps:
- task: NuGetToolInstaller@1

- task: NuGetCommand@2
  inputs:
    restoreSolution: '$(solution)'

# 🔹 1️⃣ Preparar análisis con SonarQube
- task: SonarQubePrepare@7
  inputs:
    SonarQube: 'SonarQube'  # ⚠️ Reemplaza con el nombre de tu conexión en Azure DevOps
    scannerMode: 'dotnet'
    projectKey: 'ControlAsistencia_XCF_App_Control_Asistencia_f2a51de7-37ac-469b-954d-9e549b24489a'
    projectName: 'ControlAsistencia_XCF_App'
    extraProperties: |
      sonar.host.url=http://localhost:9000

# 🔹 2️⃣ Compilar solución
- task: VSBuild@1
  inputs:
    solution: '$(solution)'
    msbuildArgs: '/p:DeployOnBuild=true /p:WebPublishMethod=Package /p:PackageAsSingleFile=true /p:SkipInvalidConfigurations=true /p:DesktopBuildPackageLocation="$(build.artifactStagingDirectory)\WebApp.zip" /p:DeployIisAppPath="Default Web Site"'
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'

# 🔹 3️⃣ Ejecutar análisis de código con SonarQube
- task: SonarQubeAnalyze@7

# 🔹 4️⃣ Publicar resultados del análisis en SonarQube
- task: SonarQubePublish@7
  inputs:
    pollingTimeoutSec: '300'

# 🔹 5️⃣ Ejecutar pruebas unitarias
- task: VSTest@2
  inputs:
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'
